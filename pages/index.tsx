// pages/index.tsx
import React from 'react';

const HomePage = () => {
    return (
        <div>
            <h1>Featured Streams</h1>
            {/* TODO: Add components for featured streams */}
        </div>
    );
};

export default HomePage;